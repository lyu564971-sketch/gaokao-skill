# 雪峰Agent 录屏教程脚本

> 总时长约 8-10 分钟，按步骤分段录制，方便剪辑

---

## 第一段：装 Python（1.5 分钟）

**画面**：浏览器 → python.org → 下载 → 安装

| 时间 | 画面 | 要说的话 |
|------|------|---------|
| 0:00 | 浏览器打开 python.org | "首先装 Python。打开 python.org，点这个黄色大按钮下载。2026 年官方改了，下载的是一个安装管理器，不是以前那种 exe 安装包了。" |
| 0:15 | 下载中 | "等几秒下载完，文件叫 python-manager，msix 格式。" |
| 0:25 | 双击安装包 | "双击打开，点安装就行。不需要像以前那样勾选 Add to PATH——新版自动帮你配好了。" |
| 0:40 | 安装进度 | "秒装完。装好之后可以验证一下：Win+R 输入 cmd，输入 python --version，看到 3.14 就说明 OK。" |
| 0:55 | 验证完成 | "到这一步 Python 就好了。不需要任何额外配置。" |

**要点强调**：2026 年新版 Python Install Manager 自动配 PATH，不需要手动勾选。

---

## 第二段：下载雪峰Agent（1 分钟）

**画面**：GitHub → Download ZIP → 解压

| 时间 | 画面 | 要说的话 |
|------|------|---------|
| 0:00 | 浏览器打开项目主页 | "然后打开 github.com/ziqihe10-droid/xuefeng-agent，这就是雪峰Agent的项目主页。"
| 0:10 | 滚到右侧 | "右上角能看到 Star 数——已经有 XXX 个 Star 了，这些都是用过觉得好用的人点的。" |
| 0:20 | 点绿色 Code 按钮 | "点这个绿色 Code 按钮，然后点 Download ZIP。" |
| 0:30 | 下载完成 | "下载完把它解压到桌面。右键→解压到当前文件夹，或者直接双击打开拖出来。" |
| 0:45 | 打开解压后的文件夹 | "解压完打开文件夹，里面这些文件等一下会用到。" |

**要点强调**：不要去别的地方下载，GitHub 是唯一官方渠道，防止别人改了代码加料。

---

## 第三段：搞 DeepSeek Key（1.5 分钟）

**画面**：platform.deepseek.com → 注册 → 创建 Key

| 时间 | 画面 | 要说的话 |
|------|------|---------|
| 0:00 | 浏览器打开 platform.deepseek.com | "然后去搞一个 AI 的钥匙。打开 platform.deepseek.com，注册登录。" |
| 0:15 | 注册流程 | "手机号注册就行，新用户送 500 万 token 免费额度，够用很久。" |
| 0:30 | 登录后界面 | "进来之后左边点 API Keys。" |
| 0:40 | 创建 Key | "点创建 API Key，随便起个名字，点确定。" |
| 0:50 | 复制 Key | "这串 sk- 开头的东西**只显示一次**，赶紧复制保存好。丢了只能重新创建。" |
| 1:10 | 强调 | "这个 Key 是扣你的免费额度的，**不要发给任何人**，不要截图发群里。" |

**要点强调**：Key 只显示一次，别发群里。

---

## 第四段：启动（1.5 分钟）

**画面**：文件夹 → 双击 bat → 黑窗口 → 双击 html → 浏览器

| 时间 | 画面 | 要说的话 |
|------|------|---------|
| 0:00 | 回到解压的文件夹 | "回到刚才解压的文件夹。先双击 `启动.bat`。" |
| 0:10 | 黑窗口出现 | "弹出来一个黑窗口，不要慌——这是服务器在启动。看到这行 `雪峰Agent: http://127.0.0.1:8765/` 和 `数据库: 已加载` 就说明跑起来了。" |
| 0:25 | 强调 | "**这个黑窗口不要关**，关了服务就停了。" |
| 0:35 | 双击打开我.html | "然后双击 `打开我.html`，浏览器会自动跳转到主界面。" |
| 0:50 | 浏览器显示聊天界面 | "看到这个聊天界面就对了。左边可以建多个对话，中间是聊天区。" |
| 1:10 | 指出关键按钮 | "右上角红色 API设置按钮，点进去。" |

**要点强调**：黑窗口不要关；先启动.bat 再打开我.html，顺序不能反。

---

## 第五段：配置 API（1 分钟）

**画面**：API 设置弹窗 → 填信息 → 保存测试

| 时间 | 画面 | 要说的话 |
|------|------|---------|
| 0:00 | API设置弹窗 | "这里填四个东西。" |
| 0:05 | Base URL | "第一行 Base URL 填 `https://api.deepseek.com`，注意是 deepseek 不是 deepseek。" |
| 0:15 | API Key | "第二行把刚才复制的 sk- 开头的 Key 粘贴进去。" |
| 0:25 | Model | "第三行 Model 填 `deepseek-chat`，这是 DeepSeek 的通用模型，够用。" |
| 0:35 | Tavily Key | "第四行 Tavily Key 选填。推荐花一分钟去 tavily.com 注册一个，填了之后 AI 能联网搜最新分数线。不填也能用，只是只能查本地数据库。" |
| 0:50 | 保存并测试 | "点保存并测试，看到绿色连接OK就大功告成了。" |

**要点强调**：Base URL 别多填 `/v1`；Key 别漏了 `sk-` 前缀。

---

## 第六段：开问演示（2 分钟）

**画面**：打字提问 → 展示回复效果

| 时间 | 画面 | 要说的话 |
|------|------|---------|
| 0:00 | 输入框打字 | "现在就可以开问了。跟微信聊天一样打字。" |
| 0:10 | 输入示例问题 | "比如：`浙江物理类655分，位次10500，想学计算机软件电子，帮我盘冲稳保`" |
| 0:20 | 发送，等回复 | "发送之后等几秒，AI 会先去数据库查，然后联网搜，最后整理回复。" |
| 0:35 | 展示回复 | "看到了吗，冲稳保三档，每个学校都标注了真实录取分数和位次，来自官方考试院数据。" |
| 0:50 | 追问演示 | "还可以追问——`电气呢？和计算机比哪个好就业？` 不用重复你前面说过的话，它记得。" |
| 1:10 | 娱乐模式 | "顶部可以切到娱乐模式，变成了张雪峰本人跟你聊。试一下——`500分学新闻有前途吗`" |
| 1:30 | 展示娱乐回复 | "看到没，那个味儿就来了。不过娱乐模式只是好玩，志愿分析还是要切回报考模式。" |
| 1:50 | 总结 | "到这一步你已经完全掌握了。以后每次用就是三步：双击启动.bat → 双击打开我.html → 开问。" |

---

## 收尾（30 秒）

| 时间 | 画面 | 要说的话 |
|------|------|---------|
| 0:00 | 回到 GitHub 页面 | "如果觉得有用，去 GitHub 点个 Star，让更多考生看到。有问题去 GitHub Issues 留言，或者抖音搜 OLO 找我。" |
| 0:15 | 展示抖音二维码/主页 | "抖音号放这里，关注获取最新更新和志愿填报干货。" |
| 0:25 | 结束 | "祝你金榜题名，报个好志愿。" |

---

## 剪辑提醒

- 每段之间留 2 秒黑场或转场，方便后期剪
- 浏览器地址栏关键时刻给个特写（python.org、github 地址、deepseek 地址）
- Python 安装时 Add to PATH 勾选给特写 + 红框标注
- API Key 打码处理
- 每段标题用字幕叠加在画面顶部
- 如果某个步骤操作快了，后期可以慢放+字幕说明
